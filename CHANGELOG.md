# Changelog

## v0.1.2

- Added `autocom` CLI command for automatic commit watching.
- Added `--help`, `--version`, `--once`, and `--dry-run` flags.
- Added dependency checks for `git` and `rg`.
- Added Homebrew formula at `Formula/autocom.rb`.
- Added local installer script and OSS metadata files.
